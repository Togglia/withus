apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-intro
  labels:
    app: web-intro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-intro
  template:
    metadata:
      labels:
        app: web-intro
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: web-intro
              topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: web-intro
      containers:
      - name: web-intro
        image: 891377152236.dkr.ecr.ap-northeast-2.amazonaws.com/web-intro:v1.1
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80  
        readinessProbe:
          tcpSocket:
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10        
        resources:
          requests:
            memory: 50Mi
            cpu: 100m
          limits:
            memory: 50Mi
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: web-intro-service
spec:
  type: NodePort
  selector:
    app: web-intro
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
